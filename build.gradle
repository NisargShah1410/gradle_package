plugins {
    id("maven-publish")
    id("java")
}
publishing {
    repositories {
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/NisargShah1410/gradle_package")
            credentials {
                username = project.findProperty("gpr.user") ?: System.getenv("NisargShah1410")
                password = project.findProperty("gpr.key") ?: System.getenv("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")
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            from(components.java)
        }
    }
}